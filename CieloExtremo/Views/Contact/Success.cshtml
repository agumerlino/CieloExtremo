@{
    ViewData["Title"] = "Éxito";
}

<div class="container mt-5 d-flex flex-column align-items-center justify-content-center">
    <div class="card shadow-lg border-0">
        <div class="card-body text-center p-5">
            <h1 class="card-title mb-4"><i class="bi bi-emoji-smile-fill"></i> ¡Gracias por tu mensaje!</h1>
            <p class="card-text lead">Hemos recibido tu mensaje y te responderemos pronto.</p>
            <a href="@Url.Action("Index", "Productos")" class="btn btn-primary custom-font-button mt-4">Volver a la página principal</a>
        </div>
    </div>
</div>